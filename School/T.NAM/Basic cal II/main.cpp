#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define sp ' '
const ll mod = 1e9+7;
const ll maxn = 1e9;

class Solution {
public:
    int calculate(string s) {
        stack<ll> pospri;
        stack<ll> pos;
        for (ll i = s.size(); i >= 0; i--)
        {
            if (s[i] == '+' || s[i] == '-')
            {
                
            }
        }
    }
};